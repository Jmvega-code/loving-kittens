import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'loving-kittens',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
